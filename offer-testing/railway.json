{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "cd offer-testing && node scripts/start-service.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },

  "_comment_scheduled_deployments": "Railway cron jobs run the same startCommand as regular deployments. Our start-service.js script automatically detects cron runs and executes the message processor instead of the web app."
}
