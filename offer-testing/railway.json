{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },

  "_comment_scheduled_deployments": "Railway scheduled deployments must be configured in the Railway Dashboard, not in this file. Go to Railway Dashboard → Your Service → Settings → Scheduled Deployments and add: Command: 'node scripts/process-message-queue.js', Schedule: '*/5 * * * *' (every 5 minutes)"
}
